#jinja2: trim_blocks: True, lstrip_blocks: True
---
apiVersion: v1
kind: Secret
metadata:
  name: wg-metrics-secret
  namespace: wg-easy
type: Opaque
stringData:
  # Bearer token for /metrics/prometheus (see `wg_easy.metrics_token`)
  token: "{{ wg_easy.metrics_token }}"
