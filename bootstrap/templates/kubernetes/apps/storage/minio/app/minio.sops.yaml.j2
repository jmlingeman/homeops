# yamllint disable
apiVersion: v1
kind: Secret
metadata:
    name: minio-secret
    namespace: storage
type: Opaque
stringData:
    MINIO_ROOT_USER: {{ minio_root_user }}
    MINIO_ROOT_PASSWORD: {{ minio_root_password }}
