---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
spec:
  storageClassName: media_movies
  volumeName: media_movies
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 1Mi
