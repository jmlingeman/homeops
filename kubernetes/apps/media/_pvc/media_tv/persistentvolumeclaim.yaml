---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
spec:
  storageClassName: media-tv
  volumeName: media-tv
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 1Mi
