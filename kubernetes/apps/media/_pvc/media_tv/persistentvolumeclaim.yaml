---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-tv-claim
spec:
  storageClassName: media-tv-storage
  volumeName: media-tv
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 1Mi
