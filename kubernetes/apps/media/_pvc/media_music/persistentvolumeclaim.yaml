---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-music-claim
spec:
  storageClassName: media-nfs-music
  volumeName: media-music-vol
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 1Mi
